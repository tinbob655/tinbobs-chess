import React, {useEffect} from 'react';


export default function ChessBoard():React.ReactElement {

    
    //initialise the game and get a default board on load
    useEffect(() => {
    }, []);


    return (
        <React.Fragment>
        </React.Fragment>
    );
};